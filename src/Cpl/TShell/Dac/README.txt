/** @namespace Cpl::TShell::Dac

The 'Dac' namespace provide implementation(s) of Text Shell that provides a
(D)irect (A)ccess (C)ommand shell.  What actually is DAC?  DAC is set of
commands that provides an alternate interface into a application.  The  DAC
interface is not intended to be a end-user accessible interface - it is for
developer access, unit tests, Model-in-Loop testings, etc.
             
*/  


  