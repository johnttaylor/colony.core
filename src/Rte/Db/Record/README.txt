/** @file */
/** @namespace Rte::Db::Record
@htmlonly
<pre>    

The 'Record' namespace contains the interfaces, classes, infrstructure, etc.
for reading and writing records the RTE Engine's Database.  An individual record 
provides persistent storage for a single Model Point.  The Record layer reads
all records in the Database at start-up and then monitors the Model Points for
change and updates persistent storage on said change.

</pre>    
@endhtmlonly
*/  


  